package com.application.database;

/**
 * This class is responsible for marking the database connection that contains SQL queries.
 */
public @interface DatabaseQuery {
    // Just to annotate the methods that are database queries
}
